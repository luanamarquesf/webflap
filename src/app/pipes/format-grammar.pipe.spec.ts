import { FormatGrammarPipe } from './format-grammar.pipe';

describe('FormatGrammarPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatGrammarPipe();
    expect(pipe).toBeTruthy();
  });
});
