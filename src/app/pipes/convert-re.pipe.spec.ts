import { ConvertRePipe } from './convert-re.pipe';

describe('ConvertRePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertRePipe();
    expect(pipe).toBeTruthy();
  });
});
